
  # Generate Dispatch Planning Board UI

  This is a code bundle for Generate Dispatch Planning Board UI. The original project is available at https://www.figma.com/design/pr6bcLFJl75PhNFYK4ZaLd/Generate-Dispatch-Planning-Board-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  